[pytest]
testpaths = tests
asyncio_mode = auto
# Made explicit rather than left to the default, which pytest-asyncio warns is
# changing. Pinning it here means a version bump changes the pinned line and not
# the behaviour of every async fixture at once.
asyncio_default_fixture_loop_scope = function
